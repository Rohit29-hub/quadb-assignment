import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  searchBar: {
    margin: 16,
    padding: 12,
    backgroundColor: '#333',
    borderRadius: 6,
  },
  searchText: {
    color: '#666',
  },
  movieList: {
    padding: 8,
  },
});